26.06.27.1848
- removing version from svg meta-data.
- removing unused preload for svg from html.
+ when double clicking to set full-screen, also try to get screen wakelock to keep the screen awake. I added built-in time-limit to 3 minutes. turns off when full-screen turns off with double click. wouldn't work in edge or chromium. just firefox (and maybe chrome).

26.06.27.1742
+ adding a little javascript to double-click to switch full screen ON/OFF.
+ making the HTML use black background and the SVG to be having transparent background/fill. it saves on phone's battery.

26.06.27.1548
+ initial.
* using CSS to animate with transform, inside the SVG, to be able to use `will-change:transform;` which allow to accelerate rendering.
* svg must be placed inline to animate (not in `<IMG>` nor in CSS `url()`), and it is still no 100% smooth.
