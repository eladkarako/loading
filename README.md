<img src="favicon.png"/>  

a light-blue circle animation, where the circle gets increased in size for 3 seconds and 500 milliseconds, then decreased in size for 3 seconds and 500 milliseconds, total of 7 seconds for the whole animation. eased in and out. animation usually used for page-loading, or practicing breathing, similar to 'Wim Hof Method'.

<hr/>

it uses css animation inside the svg using transform, which is slightly smoother, GPU wise.  
it probably won't load correctly or animate through css `url()` nor through `img src=`  
it will animate if you'll include the svg inline the html.
